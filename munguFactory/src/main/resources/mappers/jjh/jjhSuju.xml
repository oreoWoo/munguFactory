<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oracle.munguFactory.mappers.jjh.jjhSuju">
	<select id="sujuDetail" parameterType="int" resultType="OrdersDetail">
		select distinct od.Suju_No, od.item_no, i.item_Name, od.Suju_Amount, po.Prod_State
		from orders_detail od, item i, production_order po
		where od.Suju_No= #{suju_no}
		and od.item_no = i.item_no
		and po.suju_no = od.Suju_No
	</select>
	
</mapper>